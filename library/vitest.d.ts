export namespace vitest {
    namespace test {
        const global: boolean;
        namespace coverage {
            const excludeNodeModules: boolean;
            const reporter: string[];
            const reportsDirectory: string;
            const exclude: string[];
        }
    }
}
